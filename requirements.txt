# Core deep learning
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.1.0        # YOLOv8

# Data handling / augmentation
albumentations>=1.4.0
opencv-python>=4.9.0
numpy>=1.26.0
pandas>=2.2.0
pillow>=10.2.0

# Annotation format conversion (VOC XML <-> YOLO txt)
lxml>=5.1.0

# Kalman / numerical
scipy>=1.12.0
filterpy>=1.4.5           # KF/EKF reference implementation, useful for sanity-checking custom code

# Edge inference export (Component 5, scaffolded now)
onnx>=1.15.0
onnxruntime>=1.17.0
# tensorrt installed separately via NVIDIA's index when on target hardware

# Experiment tracking / config
pyyaml>=6.0.1
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0

# Notebook environment
jupyter>=1.0.0
ipykernel>=6.29.0

# Dev / quality
pytest>=8.0.0
